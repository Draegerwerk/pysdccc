codecov:
    notify:
        # do not notify until at least 24 builds (python versions * os * extension = 4 * 3 * 2 = 24) have been uploaded from the CI pipeline
        after_n_builds: 24 # https://docs.codecov.com/docs/codecovyml-reference#codecovnotify
